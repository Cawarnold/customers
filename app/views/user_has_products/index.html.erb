<h1>Listing user_has_products</h1>

<table>
  <thead>
    <tr>
      <th>Platform</th>
      <th>Service</th>
      <th>Tier</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_has_products.each do |user_has_product| %>
      <tr>
        <td><%= user_has_product.platform %></td>
        <td><%= user_has_product.service %></td>
        <td><%= user_has_product.tier %></td>
        <td><%= link_to 'Show', user_has_product %></td>
        <td><%= link_to 'Edit', edit_user_has_product_path(user_has_product) %></td>
        <td><%= link_to 'Destroy', user_has_product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User has product', new_user_has_product_path %>
